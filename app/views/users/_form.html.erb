<%= form_for(user) do |f| %>
    <% if user.errors.any? %>
        <div id="error_explanation">
            <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>
            <ul>
                <% user.errors.full_messages.each do |message| %>
                <li><%= message %></li>
                <% end %>
            </ul>
        </div>
    <% end %>

    <div class="form-group">
        <%= f.label "Email", :for => "formGroupExampleInput" %>
        <%= f.text_field :email, :class => "form-control", :id => "formGroupExampleInput2", :placeholder => "Ex : email@xyz.com" %>
    </div>
    <div class="form-group">
        <%= f.label "Mot de passe", :for => "formGroupExampleInput" %>
        <%= f.text_field :password, :class => "form-control", :id => "formGroupExampleInput2", :placeholder => "Mot de passe" %>
    </div>
    <div class="form-group">
        <%= f.label "Confirmation du mot de passe", :for => "formGroupExampleInput" %>
        <%= f.text_field :password_confirmation, :class => "form-control", :id => "formGroupExampleInput2", :placeholder => "Confirmation du mot de passe" %>
    </div>

    <%= f.submit "Créer le compte", :class => "btn btn-primary btn-lg btn-block" %>

<% end %>

